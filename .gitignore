*.env*
!*.env-example
**/.DS_Store
.vscode/**
backend/.dccache
*.sql
!*_test_data.sql
!*_test_data_empire.sql
!*_test_data_cfacts_empire.sql
!backend/docs/*.sql
backend/compose-fixed.yml
backend/compose-dev.yml
backend/dev.compose.env

# Per-worktree Makefile overrides (env fragments Makefile.main/Makefile.impl
# are checked in; Makefile.local is for ad-hoc local tweaks).
Makefile.local
tmp/
backend/lambda

# IDE and development files
.vscode/
CLAUDE.md
AGENTS.md
.claude/

# Per-worktree git hooks (not shared)
.githooks/

# Test coverage files
coverage.out
coverage.html
*.coverprofile

# Terraform state and local files
*.tfstate
*.tfstate.backup
*.tfstate.*.backup
.terraform/
.terraform.lock.hcl
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
*.tfplan

# Build artifacts and binaries
bootstrap
*.zip
!*.zip.example

# Playwright scratch output (accessibility snapshots, console logs,
# screenshots). Generated by local @playwright/mcp runs during dev testing.
.playwright-mcp/
